<%= form_for(@receipt_item) do |f| %>
  <% if @receipt_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@receipt_item.errors.count, "error") %> prohibited this receipt_item from being saved:</h2>

      <ul>
      <% @receipt_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :receipt_id %><br>
    <%= f.number_field :receipt_id %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :item_count %><br>
    <%= f.number_field :item_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
